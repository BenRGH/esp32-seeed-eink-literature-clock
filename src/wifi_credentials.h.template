#pragma once

// Copy this file to src/wifi_credentials.h and fill in your credentials.
// src/wifi_credentials.h is git-ignored.

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASS "YOUR_WIFI_PASSWORD"
